apiVersion: gateway.kyma-project.io/v1beta1
kind: APIRule
metadata:
  name: ummeta
spec:
  gateway: kyma-gateway.kyma-system.svc.cluster.local
  host: ummeta
  service:
    name: ummeta
    port: 80
  rules:
    - path: /.*
      accessStrategies:
        - config: {}
          handler: noop
      methods:
        - GET
